#!/bin/sh
set -eu
RUNTIME="$(cd "$(dirname "$0")/.." && pwd)/target"
HELPER="$RUNTIME/bin/helper/container-runtime-helper.x86_64"
[ -u "$HELPER" ] || { echo "NVIDIA Container Runtime: setuid helper missing" >&2; exit 1; }
exec "$HELPER" postinst
