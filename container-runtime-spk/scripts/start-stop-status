#!/bin/sh
RUNTIME="$(cd "$(dirname "$0")/.." && pwd)/target"
HELPER="$RUNTIME/bin/helper/container-runtime-helper.x86_64"
case "${1:-}" in
  start) [ -u "$HELPER" ] && exec "$HELPER" start || exit 1 ;;
  status) [ -u "$HELPER" ] && exit 0 || exit 1 ;;
  stop) exit 0 ;;
  *) exit 1 ;;
esac
